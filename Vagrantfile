# -*- mode: ruby -*-
# vi: set ft=ruby :

# This Vagrantfile is for convenience in developing the lens for Augeas.
# It is not required if your system can already run Augeas.

Vagrant.configure('2') do |config|
  config.vm.box = "centos-64-x64-vbox4210-nocm"
  # config.vm.box_url = "http://domain.com/path/to/above.box"
end
